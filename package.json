{
  "name": "rupee-test",
  "version": "1.0.0",
  "description": "Automação de testes do Rupee",
  "main": "/src/app.js",
  "scripts": {
    "dev": "tsnd --transpile-only --respawn --ignore-watch node_modules -r tsconfig-paths/register src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Wagner Junior",
  "license": "ISC",
  "dependencies": {
    "@sparticuz/chromium": "^113.0.1",
    "puppeteer-core": "^20.1.2"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "tscpaths": "^0.0.9",
    "typescript": "^5.0.4"
  }
}
